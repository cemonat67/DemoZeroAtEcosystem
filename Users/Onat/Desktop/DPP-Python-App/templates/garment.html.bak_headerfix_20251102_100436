{% extends "base.html" %}

{% block title %}Garment DPP - Rabateks{% endblock %}

{% block content %}
<div class="row">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1><i class="fas fa-tshirt me-2"></i>Garment Digital Product Passport</h1>
            <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addGarmentModal">
                <i class="fas fa-plus me-1"></i>Add New Garment
            </button>
        </div>
    </div>
</div>

<!-- Statistics Cards -->
<div class="row mb-4">
    <div class="col-md-3">
        <div class="card bg-primary text-white">
            <div class="card-body">
                <h4>{{ stats.total_count }}</h4>
                <p class="mb-0">Total Garments</p>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-success text-white">
            <div class="card-body">
                <h4>{{ "{:,}".format(stats.total_quantity) }}</h4>
                <p class="mb-0">Total Quantity</p>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-warning text-white">
            <div class="card-body">
                <h4>{{ stats.unique_styles }}</h4>
                <p class="mb-0">Unique Styles</p>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-info text-white">
            <div class="card-body">
                <h4>{{ stats.avg_weight }}g</h4>
                <p class="mb-0">Avg Weight</p>
            </div>
        </div>
    </div>
</div>

<!-- Garments Table -->
<div class="card">
    <div class="card-header">
        <h5 class="mb-0">Garment Inventory</h5>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped" id="garmentsTable">
                <thead>
                    <tr>
                        <th>Style Name</th>
                        <th>PO Number</th>
                        <th>Product Type</th>
                        <th>Country</th>
                        <th>Fabric Type</th>
                        <th>Weight (g)</th>
                        <th>Quantity</th>
                        <th>Sustainability</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for garment in garments %}
                    <tr>
                        <td><strong>{{ garment.style_name }}</strong></td>
                        <td>{{ garment.po_number }}</td>
                        <td>{{ garment.product_type }}</td>
                        <td>{{ garment.country }}</td>
                        <td>{{ garment.fabric_type }}</td>
                        <td>{{ garment.fabric_weight }}</td>
                        <td>{{ "{:,}".format(garment.quantity) }}</td>
                        <td>
                            <div class="progress" style="height: 20px;">
                                <div class="progress-bar bg-{{ 'success' if garment.sustainability_score >= 70 else 'warning' if garment.sustainability_score >= 40 else 'danger' }}" 
                                     style="width: {{ garment.sustainability_score }}%">
                                    {{ garment.sustainability_score }}%
                                </div>
                            </div>
                        </td>
                        <td>
                            <button class="btn btn-sm btn-outline-primary" onclick="viewGarment({{ garment.id }})">
                                <i class="fas fa-eye"></i>
                            </button>
                            <button class="btn btn-sm btn-outline-danger" onclick="deleteGarment({{ garment.id }})">
                                <i class="fas fa-trash"></i>
                            </button>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Add Garment Modal -->
<div class="modal fade" id="addGarmentModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Add New Garment</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="addGarmentForm">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">Style Name</label>
                                <input type="text" class="form-control" name="style_name" required>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">PO Number</label>
                                <input type="text" class="form-control" name="po_number" required>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">Product Type</label>
                                <select class="form-control" name="product_type" required>
                                    <option value="">Select Type</option>
                                    <option value="T-Shirt">T-Shirt</option>
                                    <option value="Polo Shirt">Polo Shirt</option>
                                    <option value="Dress Shirt">Dress Shirt</option>
                                    <option value="Casual Shirt">Casual Shirt</option>
                                    <option value="Blouse">Blouse</option>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">Country</label>
                                <input type="text" class="form-control" name="country" required>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">Fabric Type</label>
                                <input type="text" class="form-control" name="fabric_type" required>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">Fabric Weight (g)</label>
                                <input type="number" class="form-control" name="fabric_weight" step="0.1">
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">Quantity</label>
                                <input type="number" class="form-control" name="quantity" required>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label class="form-label">Sustainability Score (0-100)</label>
                                <input type="number" class="form-control" name="sustainability_score" min="0" max="100" value="50">
                            </div>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Production Facility</label>
                        <input type="text" class="form-control" name="production_facility" required>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary" onclick="saveGarment()">Save Garment</button>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script>
function saveGarment() {
    const form = document.getElementById('addGarmentForm');
    const formData = new FormData(form);
    const data = Object.fromEntries(formData);
    
    fetch('/garment/api/garments', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify(data)
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            location.reload();
        } else {
            alert('Error: ' + data.message);
        }
    })
    .catch(error => {
        alert('Error: ' + error);
    });
}

function deleteGarment(id) {
    if (confirm('Are you sure you want to delete this garment?')) {
        fetch(`/garment/api/garments/${id}`, {
            method: 'DELETE'
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                location.reload();
            } else {
                alert('Error: ' + data.message);
            }
        });
    }
}

function viewGarment(id) {
    fetch(`/garment/api/garments/${id}`)
    .then(response => response.json())
    .then(data => {
        alert(`Garment Details:\n\nStyle: ${data.style_name}\nPO: ${data.po_number}\nType: ${data.product_type}\nCountry: ${data.country}\nFabric: ${data.fabric_type}\nWeight: ${data.fabric_weight}g\nQuantity: ${data.quantity}\nSustainability: ${data.sustainability_score}%`);
    });
}
</script>
{% endblock %}